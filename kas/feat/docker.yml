header:
  version: 14

local_conf_header:
  docker: |
    DISTRO_FEATURES:append = " \
     docker \
     k8s \
    "
repos:
  meta-virtualization:
    url: git://git.yoctoproject.org/meta-virtualization.git
    branch: scarthgap